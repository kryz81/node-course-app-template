const { v4 } = require('uuid');

const save = async data => {
  // create an user object here:
  // - generate id with v4
  // - hash password with shared/password module
  // - import and use model to save data to mongodb
};

module.exports = {
  save,
};
